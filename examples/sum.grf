third = projector<2, 3>
third_plus = composition<successor, third>
//third_plus(x, y, z) -> z + 1

id = projector<0, 1>
//id(x) -> x

sum = recursion<id, third_plus>
//sum(x, 0) -> id(x) -> x
//sum(x, y + 1) 
//		-> third_plus(x, y, sum(x, y))
//				-> sum(x, y) + 1
sum